# --- Banco de Dados (Railway Postgres) ---
# Use uma única variável DATABASE_URL em formato JDBC:
# Exemplo (a partir do DATABASE_URL da Railway, adicionando o prefixo jdbc:):
# DATABASE_URL=jdbc:postgresql://host:port/railway?sslmode=require
DATABASE_URL=jdbc:postgresql://host:port/railway?sslmode=require

# --- URL pública da aplicação ---
# Em produção use algo como:
# PUBLIC_URL=https://seu-servico-production.up.railway.app
PUBLIC_URL=http://localhost:8080/

# --- Autenticação da UI ---
KESTRA_USERNAME=admin@example.com
KESTRA_PASSWORD=change-me-please

# --- Logs (mesmo padrão do template referência) ---
LOGGER_LEVELS_ROOT=INFO
LOGGER_LEVELS_ORG_FLYWAYDB=INFO

# --- AI Copilot (Gemini) ---
GEMINI_API_KEY=sua-gemini-api-key-aqui
GEMINI_MODEL=gemini-2.5-flash


# --- Git Sync (flows via Git) ---
# URL do repositório Git que contém os flows (e opcionalmente namespaces).
GIT_SYNC_URL=https://github.com/sua-org/seu-repo-flows.git

# Branch a ser sincronizada (por exemplo: main, dev).
GIT_SYNC_BRANCH=main

# Namespace alvo onde os flows serão escritos (ex: default, prod, dev).
GIT_SYNC_TARGET_NAMESPACE=default

# Diretório dentro do repo onde ficam os arquivos de flows.
GIT_SYNC_DIRECTORY=_flows

# --- Autenticação opcional do Git (descomente conforme sua necessidade) ---
# HTTPS com usuário + PAT/token:
# GIT_SYNC_USERNAME=seu-usuario-ou-bot
# GIT_SYNC_PASSWORD=seu-token-pessoal

# SSH com chave privada:
# GIT_SYNC_SSH_KEY="-----BEGIN OPENSSH PRIVATE KEY-----\n...\n-----END OPENSSH PRIVATE KEY-----"
# GIT_SYNC_KNOWN_HOSTS=github.com ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA...

